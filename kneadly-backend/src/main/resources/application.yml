spring:
  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        profile:
          name: default
  datasource:
    url: jdbc:postgresql://database-1.cfkwxftmjmkh.us-east-1.rds.amazonaws.com:5432/postgres
    driver-class-name: org.postgresql.Driver
    username: postgresKneadly
    password: Kneadly123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true